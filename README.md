# Тестовое задание: Junior Golang Developer (Effective Mobile)

### Задача
Спроектировать и реализовать REST-сервис для агрегации данных об онлайн-подписках пользователей.

## 🛠 Технологический стек
* **Язык:** Go (версия 1.25.5)
* **Фреймворк:** Fiber (v2)
* **База данных:** PostgreSQL
* **Документация:** Swagger
* **Конфигурация:** .yaml файл
* **Контейнеризация:** Docker & Docker Compose

## Запуск
* запусть `make up-build` в командной строке Linux
* Открыть Swagger-документацию http://100.96.10.2:5015/swagger

### Требования
1. **HTTP-ручки для CRUDL-операций** над записями о подписках. Каждая запись должна содержать:
    * Название сервиса, предоставляющего подписку
    * Стоимость месячной подписки в рублях (целое число)
    * ID пользователя в формате UUID
    * Дата начала подписки (месяц и год)
    * Опционально: дата окончания подписки
2. **HTTP-ручка для подсчета суммарной стоимости** всех подписок за выбранный период с возможностью фильтрации по:
    * `user_id`
    * Названию подписки
3. **СУБД – PostgreSQL**:
    * Наличие миграций для инициализации схемы базы данных.
4. **Логирование**:
    * Весь основной код должен быть покрыт логами.
5. **Конфигурация**:
    * Вынос настроек в `.env` или `.yaml` файл.
6. **Документация**:
    * Предоставить Swagger-документацию к реализованному API.
7. **Развертывание**:
    * Запуск сервиса одной командой с помощью `docker compose`.

### Примечания
* Проверка существования пользователя не требуется.
* Стоимость подписки — только целое число (без копеек).
* Пример тела запроса на создание записи:
```json
{
  "service_name": "Yandex Plus",
  "price": 400,
  "user_id": "60601fee-2bf1-4721-ae6f-7636e79a0cba",
  "start_date": "07-2025"
}

